<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plataforma Prof. Jubilut – API</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 640px; margin: 2rem auto; padding: 0 1rem; color: #1e293b; }
        h1 { font-size: 1.5rem; }
        a { color: #2563eb; }
        code { background: #f1f5f9; padding: 0.2em 0.4em; border-radius: 4px; font-size: 0.9em; }
        ul { line-height: 1.8; }
    </style>
</head>
<body>
    <h1>Plataforma Prof. Jubilut – Backend API</h1>
    <p>Aplicação em modo API + SPA. A interface fica no frontend (Vue).</p>
    <ul>
        <li><a href="{{ url('/docs') }}">Documentação da API</a> (<code>/docs</code>)</li>
        <li>Frontend SPA: configure <code>FRONTEND_URL</code> no <code>.env</code> (ex.: <code>http://localhost:5173</code>)</li>
    </ul>
</body>
</html>
